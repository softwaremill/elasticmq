queues-storage {
  enabled = true
  path = ""
}

queues {
 test-2 {contentBasedDeduplication=true,copyTo=copyTo,deadLettersQueue{maxReceiveCount=4,name=dead},defaultVisibilityTimeout=3000,delay=0,fifo=false,moveTo=messageTo,receiveMessageWait=0,tags{tag1Key=tag1Value}}
 test-3 {contentBasedDeduplication=true,copyTo=copyTo3,deadLettersQueue{maxReceiveCount=4,name=dead-3},defaultVisibilityTimeout=3000,delay=0,fifo=false,moveTo=messageTo3,receiveMessageWait=0,tags{tag13Key=tag13Value}}
}
