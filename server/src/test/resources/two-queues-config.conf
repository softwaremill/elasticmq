queues {
 test {contentBasedDeduplication=true,copyTo=copyTo,deadLettersQueue{maxReceiveCount=4,name=dead},defaultVisibilityTimeout=3000,delay=0,fifo=false,moveTo=messageTo,receiveMessageWait=0,tags{tag1Key=tag1Value}}
 test-2 {contentBasedDeduplication=true,copyTo=copyTo2,deadLettersQueue{maxReceiveCount=4,name=dead-2},defaultVisibilityTimeout=3000,delay=0,fifo=false,moveTo=messageTo2,receiveMessageWait=0,tags{tag12Key=tag12Value}}
}