akka {
    loglevel = "DEBUG"

    // TODO: REMOVE
    actor {
        debug {
            receive = on
            lifecycle = on
            autoreceive = on
        }
    }
}

elasticmq {
    debug = true
}

akka.io.tcp.trace-logging = on